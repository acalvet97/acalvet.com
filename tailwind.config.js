/** @type {import('tailwindcss').Config} */
export default {
  content: [],
  theme: {
    extend: {
      fontFamily: {
        sans: ["Geist", "sans-serif"], // Changed to match @font-face name
        mono: ["Geist", "monospace"],  // Changed to match @font-face name
      },
    },
  },
  plugins: [],
}
